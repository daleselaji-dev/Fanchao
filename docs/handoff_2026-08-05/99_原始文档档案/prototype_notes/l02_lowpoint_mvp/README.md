# L02《落底》最小体验原型

一个隔离的 45 秒可走动验证，不使用旧 H02 的群众同步或整层海床。

运行：

```bash
python3 -m http.server 4175 --bind 127.0.0.1
```

打开 `http://127.0.0.1:4175/prototypes/l02_lowpoint_mvp/`，点击“进入”，使用 `WASD` 或方向键靠近四人桌。`?automation=1` 直接进入第 31 秒，供画面审查。

通过条件：玩家能在不读文案的情况下意识到，食堂的最低处沿地面、桌腿和墙砖迁移；它不是水、泥、洞、尸体或家具怪。
