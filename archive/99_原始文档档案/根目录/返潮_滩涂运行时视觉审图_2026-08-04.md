# 《返潮》滩涂运行时视觉审图：2026-08-04

## 本轮证据

- Unreal 编译：`MudflatDemoEditor Win64 Development`，成功。
- 自动演示回归：`G:\FanChao\FanChaoProject\output\mudflat_runtime_verify_20260804_220838.stdout.log`。
- 回归结果：`EngineStarted=True`、`IntroMessage=True`、`AutoAlarm1=True`、`AutoAlarm2=True`、`MUDflat_RUNTIME_VERIFY=PASS`。
- 720p 独立运行审图：`G:\FanChao\FanChaoProject\FanchaoMudflatUE\Saved\Screenshots\WindowsEditor\HighresScreenshot00010.png`。

## 已通过的技术证据

- 独立地图可以启动并进入滩涂运行时。
- 自动审查模式改为显式 `-RetideAutoDemo`，普通启动不再自动替玩家触发警报。
- 手动模式的 `E` 只允许在当前有效控制箱附近触发；离开范围会记录 `INPUT_REJECTED`。
- 第二次警报后，玩家必须到警报塔附近才进入 `Done`，路线闭环不再依赖第二次按键时恰好站在塔旁。
- 迁移的控制箱 GLB 上下壳已校正为同一局部原点，正面按键与显示窗在 `-Y` 审图机位可见。
- 玩家出生点和审图固定机位已从旧灰盒的错误位置移到第一个控制箱路线。
- 家属可见组件现在挂接到所属 Actor 根节点，离滩动画会移动真实几何而不是留下漂浮部件。

## 视觉 Gate 结果：REJECTED

运行时截图仍然不能称为 PS3 级成品画面：

- 滩涂主体仍是基础立方体/圆柱体，出现明显的漂浮测试组件和大片空白地面。
- 环境没有真实的沿海工作场所功能，不能先读成日常共同体。
- 角色仍是临时骨架外观；M01 父亲的三视图 Gate 仍为 `REJECTED`，不能接入。
- 当前画面只证明控制箱近景资产可读，不证明场景整体通过审图门。

## 下一轮制作顺序

1. 保留已校正的控制箱组件和两个运行时模式。
2. 用真实可授权的滩涂/海防设施构件替换压力方块、漂浮碎片和临时人形；先完成一个可被 720p 读出的近景工作区。
3. 加入同一空间的干燥受压证据：墙/柜/地面/胶条或沉积物只能表达一个不可见高度，不使用水、蓝雾或海洋装饰。
4. 继续寻找通过 Gate 的连续成年男性工人网格；在此之前禁止 UV、PBR、贴图和 Unreal 导入。
