{
  "name": "fanchao",
  "version": "0.1.0",
  "private": true,
  "description": "Fanchao - a small full-stack stir-fry recipe board used as the reference app for the Cloud Agent development environment.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w server && npm run build -w client",
    "test": "npm run test -w server",
    "lint": "eslint .",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "concurrently": "^9.1.0",
    "eslint": "^8.57.1",
    "eslint-plugin-react": "^7.37.2",
    "eslint-plugin-react-hooks": "^4.6.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
